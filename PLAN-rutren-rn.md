# RuTren React Native — План и Задачи

---

## 📋 Task Checklist

# RuTren React Native — Task Checklist

## Phase 0: Подготовка и планирование

- [x] Исследовать текущую кодовую базу TMA
- [x] Определить архитектуру будущего RN-приложения
- [x] Написать implementation plan и получить одобрение

## Phase 1: Инициализация проекта

- [ ] Создать Expo проект (`npx create-expo-app`)
- [ ] Настроить TypeScript, ESLint, Prettier
- [ ] Установить зависимости (Zustand, Firebase, React Navigation, Reanimated, etc.)
- [ ] Настроить алиасы путей (`@/`)
- [ ] Подключить шрифт Outfit (expo-font / Google Fonts)

## Phase 2: Shared-слой (переиспользуемый код)

- [ ] Перенести типы (`types/`) — прямой копипаст
- [ ] Перенести Zustand-сторы (`stores/`) — прямой копипаст
- [ ] Перенести данные (`data/`) — прямой копипаст
- [ ] Перенести утилиты (`utils/`) — адаптация (убрать DOM-зависимости)
- [ ] Настроить Firebase SDK для React Native

## Phase 3: Дизайн-система (UI-компоненты)

- [ ] Создать файл дизайн-токенов (`theme.ts`)
- [ ] `GlassCard` → NativeWind/StyleSheet glassmorphism
- [ ] `NeonButton` → Pressable + Reanimated анимации
- [ ] `WeightInput` → TextInput с кастомным стилем
- [ ] `GlassySkeleton` → Reanimated pulse-анимация
- [ ] `BottomTabBar` → React Navigation Bottom Tabs
- [ ] `PageHeader` → SafeAreaView + кастомный хедер
- [ ] `Toast` → react-native-toast-message или кастом

## Phase 4: Навигация

- [ ] Настроить React Navigation (Stack + Bottom Tabs)
- [ ] Описать все маршруты и типизировать навигацию
- [ ] Bottom Tabs: Dashboard, Workouts, Profile
- [ ] Stack navigators для вложенных экранов
- [ ] Анимации переходов (shared element transitions)

## Phase 5: Экраны (миграция Views)

### Приоритет 1 — Core Flow

- [ ] Dashboard (дашборд)
- [ ] Workouts (список программ)
- [ ] Active Workout (активная тренировка)
- [ ] Workout Summary (итоги)
- [ ] Onboarding

### Приоритет 2 — Profile & Data

- [ ] Profile
- [ ] Personal Data
- [ ] Measurements
- [ ] Settings

### Приоритет 3 — Extended Features

- [ ] Exercises Library
- [ ] Exercise Details
- [ ] History
- [ ] History Detail
- [ ] Habits
- [ ] Analytics
- [ ] Achievements
- [ ] Create / Edit Workout

## Phase 6: Нативные интеграции

- [ ] Haptics (`expo-haptics`)
- [ ] Push-уведомления (`expo-notifications`)
- [ ] Secure storage (`expo-secure-store`)
- [ ] Splash screen (`expo-splash-screen`)
- [ ] Status bar кастомизация

## Phase 7: Полировка

- [ ] Анимации (Reanimated 3 + Gesture Handler)
- [ ] Dark theme (полная поддержка)
- [ ] Performance оптимизация (FlatList, memo, lazy)
- [ ] Error boundaries
- [ ] Offline-режим (persist Zustand + MMKV)
- [ ] Тестирование на iOS + Android

## Phase 8: Сборка и деплой

- [ ] Настроить EAS Build (Expo Application Services)
- [ ] Генерация app icons и splash screen
- [ ] Тестовый билд на устройства
- [ ] Подготовка к публикации (App Store / Google Play)

---

## 🏗️ Implementation Plan

# План миграции RuTren в React Native (Expo)

Это технический план по переносу приложения RuTren из Web-окружения (Telegram Mini App на React/Vite) в полноценное мобильное приложение на React Native с использованием **Expo**.

## 🎯 Общая архитектура и стек

Мы сохраняем максимум бизнес-логики и типов из существующего TMA:

1. **Фреймворк**: React Native + Expo (Managed Workflow)
2. **Навигация**: React Navigation (Bottom Tabs + Native Stack)
3. **Стейт-менеджер**: Zustand (с `zustand/middleware/persist` и адаптером `react-native-mmkv` для скорости)
4. **Стилизация**: `StyleSheet` (или `NativeWind` для синтаксиса Tailwind, если предпочтительнее)
5. **Анимации**: `react-native-reanimated` v3 (заменит Framer Motion)
6. **База данных / Auth**: Firebase React Native SDK (`@react-native-firebase/app` + `auth` + `firestore`)
7. **Шрифты**: Встроенный шрифт Outfit (`expo-font`)
8. **Токены дизайна**: Цвета из `design_tokens.md` (Neon Rose `#FF5D8F`, OLED-черный `#050505`)

---

## 🏗️ План реализации (Proposed Changes)

### 1️⃣ Инициализация проекта и базовые настройки

#### [NEW] `app.json` (Expo Config)

Конфигурация Expo-приложения (имя, package name, bundle id, сплеш-скрин, цвета статусбара, пути к иконкам).

#### [NEW] `src/theme/tokens.ts`

Перенос CSS-переменных из `variables.css` и `design_tokens.md` в типизированный TS-объект:

```typescript
export const Theme = {
  colors: {
    background: '#050505',
    primary: '#FF5D8F',
    card: '#18181B',
    // ...
  }
}
```

### 2️⃣ Shared-слой (Перенос без изменений)

Эти папки копируются напрямую из текущего проекта (возможно, с минимальными правками путей):

- #### [NEW] `src/types/` (User, Workout, Exercise, Habit и т.д.)

- #### [NEW] `src/data/` (Моки и большие базы упражнений: `exercises_large.ts`)

### 3️⃣ Адаптация State Management (Zustand)

Zustand работает в RN "из коробки", но нужно заменить `localStorage` (если использовался в persist) на `react-native-mmkv` или `@react-native-async-storage/async-storage`.

#### [NEW] `src/stores/*`

- `useUserStore.ts`

- `useWorkoutStore.ts`
- `useHabitStore.ts`
*(Прямой перенос логики, замена адаптера хранилища в middleware `persist`)*.

### 4️⃣ Адаптация UI-кита (Атомы и Молекулы)

Framer Motion недоступен в RN, используем Reanimated. HTML-теги заменяем на RN-компоненты.

#### [NEW] `src/components/atoms/GlassCard.tsx`

Реализация Glassmorphism в RN сложнее. Используем `expo-blur` (компонент `BlurView`) в качестве фона для карточек поверх темного фона.

#### [NEW] `src/components/atoms/NeonButton.tsx`

RN-аналог кнопки: `Pressable` с Reanimated для масштабирования (`scale: 0.95` при нажатии) и интеграцией `expo-haptics` (вместо haptics от Telegram SDK). Неоновое свечение (shadow) через свойства `shadowColor`, `shadowOpacity` или кастомные градиенты.

#### [NEW] `src/components/atoms/WeightInput.tsx`

Использование `TextInput` с пропсами `keyboardType="numeric"`, `placeholderTextColor`, снятием фокуса по клику вне инпута (`Keyboard.dismiss()`).

#### [NEW] `src/components/ui/Toast.tsx`

Использование библиотеки `react-native-toast-message` для всплывающих уведомлений, стилизованной под RuTren (темный фон, неоновый border).

### 5️⃣ Навигация (Замена react-router-dom)

#### [NEW] `src/navigation/RootNavigator.tsx`

Настройка `createBottomTabNavigator` для главных экранов (Главная, Тренировки, Профиль) и `createNativeStackNavigator` для экранов деталей (Итоги тренировки, Активная тренировка, Настройки).

### 6️⃣ Миграция Экранов (Pages)

Переписывание JSX из React DOM на React Native (`div` -> `View`, `span`/`p` -> `Text`, `img` -> `Image`, `button` -> `Pressable`/`TouchableOpacity`).

#### [NEW] Экраны первой очереди (Core Flow)

- `DashboardView` (Виджеты, WeekCalendar через `ScrollView`/`FlatList`)

- `WorkoutView` (Активная тренировка, таймер `RestTimer`)
- `WorkoutSummaryView`

#### [NEW] Экраны второй очереди

- `ProfileView`, `OnboardingView`, `SettingsView`, базы упражнений.

### 7️⃣ Утилиты и Firebase

#### [MODIFY] Утилиты форматов (`utils/`)

Прямой перенос математики (калории, TON). Удаление утилит, жестко завязанных на DOM (например `document.documentElement.classList`).

#### [NEW] `src/config/firebase.ts`

Инициализация `@react-native-firebase/app`.

---

## 🧪 Verification Plan (План тестирования)

Поскольку проект переписывается на новый фреймворк, потребуется тщательная проверка на симуляторе/реальном устройстве.

### 1. Тестирование Инициализации (Automated/Manual)

- **Команда**: `npx expo start` (или `npm run start`)

- **Ожидание**: Expo Metro Bundler запускается без ошибок. Приложение открывается в Expo Go (iOS/Android) или в iOS Simulator без белого экрана (Splash Screen отрабатывает корректно).

### 2. Тестирование Core Flow (Manual)

1. **Онбординг**: Ввод параметров тела (рост, вес). Проверка работы кастомной цифровой клавиатуры (`WeightInput`).
2. **Навигация**: Переключение между табами (Dashboard, Workouts, Profile). Проверка отсутствия лагов (60fps) и корректности отображения иконок табов.
3. **Запуск активности**: Старт новой тренировки. Ввод веса/повторов.
4. **Таймер**: Запуск `RestTimer`, убедиться, что он работает в фоне или не засыпает при активном экране (использование `expo-keep-awake`, если нужно).
5. **Завершение**: Переход на экран Summary, проверка пересчета TON и Калорий. Запись в историю.

### 3. Тестирование Native Integration (Manual on Physical Device)

1. **Haptics**: Ощутимая вибрация при нажатии на `NeonButton` и сохранении подхода.
2. **Keyboard Avoiding**: При вводе данных на экране логина/онбординга клавиатура не перекрывает поля ввода (`KeyboardAvoidingView`).
3. **Safe Area**: Контент на устройствах с "челкой" (iOS) и вырезами (Android) отрисовывается строго внутри безопасной зоны (`SafeAreaView`).
4. **Хранилище (Persist)**: Перезапуск приложения полностью сохраняет профиль пользователя и историю тренировок (тест `react-native-mmkv`).
